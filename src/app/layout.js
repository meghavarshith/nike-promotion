export const metadata = {
  title: "NIKE PRECISION 7 | Built for Control",
  description: "Experience the cinematic product launch.",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
